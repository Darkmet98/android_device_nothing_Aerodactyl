# --- Letter C1 (06/08/2026): stack MTK de rendimiento como MÓDULOS ---
# =y falla en el link LTO: powerhal_cpu_ctrl.c y touch_boost.c definen los mismos
# helpers (perfmgr_copy_from_user_for_proc, cpu_policy_init, get_cpu_opp_info...).
# Como =m cada .ko enlaza por separado (diseño MTK original).
# Test: insmod de los .ko a mano; empaquetado formal en vendor_dlkm si se valida.
CONFIG_MTK_PERFORMANCE_MODULE=m
