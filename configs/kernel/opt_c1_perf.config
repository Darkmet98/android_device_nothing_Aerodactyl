# --- Letter C1 (06/08/2026): habilitar stack MTK de rendimiento (master) ---
# Gate maestro de drivers/misc/mediatek/performance/ — trae touch_boost,
# powerhal_cpu_ctrl, perf_ioctl (NO fpsgo_v3: ese requiere MTK_CPUFREQ_SUGOV_EXT
# + MTK_SCHEDULER — cascada grande, letra aparte).
# Lineage lo deshabilitó en lineage.config (# CONFIG_MTK_PERFORMANCE_MODULE is not set)
CONFIG_MTK_PERFORMANCE_MODULE=y
